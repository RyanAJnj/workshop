package pak1;
import java.io.*;
import java.util.*;


public class WeatherStationTest {

	public static double getMean(List<Double> wS) {
		int size = 0;
		double sum = 0.0;
		try (Scanner scanner = new Scanner(new File("C:\\Users\\jimen\\Downloads\\weather_stations.txt"))) {
			while (scanner.hasNextLine()) {
				String line = scanner.nextLine(); 
				String[] parts = line.split(";");
				String city = parts[0];
				double weather = Double.parseDouble(parts[1]);
				size++;
				sum += weather;
				System.out.println(parts[0] + ": " + parts[1]);
				    }
				} catch (FileNotFoundException e) {
				    System.err.println("Can't find the file you inputted: " + e.getMessage());
				}
		double mean = sum / size;
		return mean;
	}
	
	public static void main(String[] args) {
		
		List<Double> weatherStations = new ArrayList<>();
		System.out.println("The mean of the entire file is " + getMean(weatherStations));
	}

}